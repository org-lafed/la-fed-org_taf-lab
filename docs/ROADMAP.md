# Roadmap

## Near Term

- Validate and harden remaining UI locators against the live site.
- Expand product, login, and cart coverage with stable data handling.
- Add richer API assertions and cleanup helpers.

## Later

- Introduce remote execution support for Selenium Grid or cloud providers.
- Add richer test data management and reporting metadata.
- Plug a newer Allure report renderer onto the standard `allure-results` output.
